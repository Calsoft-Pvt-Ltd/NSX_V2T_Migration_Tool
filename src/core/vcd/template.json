{
  "vCloudDirector": {
    "createOrgVDCNetwork": {
      "name": "{{orgVDCNetworkName}}",
      "description": "{{orgVDCNetworkDescription}}",
      "subnets": {
        "values": [
          {
            "gateway": "{{orgVDCNetworkGateway}}",
            "prefixLength": "{{orgVDCNetworkPrefixLength}}",
            "dnsSuffix": "{{orgVDCNetworkDNSSuffix}}",
            "dnsServer1": "{{orgVDCNetworkDNSServer1}}",
            "dnsServer2": "{{orgVDCNetworkDNSServer2}}",
            "ipRanges": {
              "values": []
            },
            "enabled": true
          }
        ]
      },
      "networkType": "{{orgVDCNetworkType}}",
      "connection": {
        "routerRef": {
          "name": "{{edgeGatewayName}}",
          "id": "{{edgeGatewayId}}"
        },
        "connectionType": "INTERNAL",
        "connected": true
      },
      "shared": false
    },
    "createOrgVDCComputePolicy": {
      "id": "{{computePolicyId}}",
      "description": "{{description}}",
      "name": "{{name}}",
      "cpuSpeed": "{{cpuSpeed}}",
      "memory": "{{memory}}",
      "cpuCount": "{{cpuCount}}",
      "coresPerSocket": "{{coresPerSocket}}",
      "memoryReservationGuarantee": "{{memoryReservationGuarantee}}",
      "cpuReservationGuarantee": "{{cpuReservationGuarantee}}",
      "cpuLimit": "{{cpuLimit}}",
      "memoryLimit": "{{memoryLimit}}",
      "cpuShares": "{{cpuShares}}",
      "memoryShares": "{{memoryShares}}",
      "extraConfigs": {
        "additionalProp1": "string",
        "additionalProp2": "string",
        "additionalProp3": "string"
      },
      "pvdcComputePolicyRef": {
        "name": "string",
        "id": "string"
      },
      "pvdcComputePolicy": {
        "name": "string",
        "id": "string"
      },
      "compatibleVdcTypes": [
        "string"
      ],
      "pvdcId": "string",
      "namedVmGroups": [
        [
          {
            "name": "string",
            "id": "string"
          }
        ]
      ],
      "logicalVmGroupReferences": [
        {
          "name": "string",
          "id": "string"
        }
      ],
      "isAutoGenerated": true
    },
    "createOrgVDCAccessControl": {
      "isSharedToEveryone": "{{isShared}}",
      "everyoneAccessLevel": "{{everyoneAccess}}"
    },
    "renameOrgVDC": {
      "name": "{{newOrgVDCName}}",
      "allocationModel": "{{allocationModel}}",
      "computeCapacity": {
        "cpu": {
          "units": "{{cpuUnits}}",
          "allocated": "{{cpuAllocated}}",
          "limit": "{{cpuLimit}}",
          "reserved": "{{cpuReserved}}",
          "used": "{{cpuUsed}}"
        },
        "memory": {
          "units": "{{memoryUnits}}",
          "allocated": "{{memoryAllocated}}",
          "limit": "{{memoryLimit}}",
          "reserved": "{{memoryReserved}}",
          "used": "{{memoryUsed}}"
        }
      },
      "nicQuota": "{{nicQuota}}",
      "networkQuota": "{{networkQuota}}",
      "providerVdcReference": {
        "href": "{{pvdcHref}}",
        "id": "{{pvdcId}}",
        "name": "{{pvdcName}}",
        "type": "{{pvdcType}}"
      }
    },
    "createDNATServices": {
      "id": "{{ruleId}}",
      "name": "{{ruleTag}}",
      "description": "{{ruleDescription}}",
      "enabled": "{{enabled}}",
      "ruleType": "{{action}}",
      "externalAddresses": "{{originalAddress}}",
      "internalAddresses": "{{translatedAddress}}",
      "logging": "{{loggingEnabled}}",
      "systemRule": false,
      "snatDestinationAddresses": " ",
      "version": {
        "version": "{{version}}"
      }
    },
    "createSNATServices": {
      "id": "{{ruleId}}",
      "name": "{{ruleTag}}",
      "description": "{{ruleDescription}}",
      "enabled": "{{enabled}}",
      "ruleType": "{{action}}",
      "externalAddresses": "{{originalAddress}}",
      "internalAddresses": "{{translatedAddress}}",
      "logging": "{{loggingEnabled}}",
      "systemRule": false,
      "snatDestinationAddresses": "{{snatDestinationAddresses}}",
      "version": {
        "version": "{{version}}"
      }
    },
    "addUplinkEdgeGateway": {
      "name": "{{edgeGatewayUplinkName}}",
      "displayName": "{{edgeGatewayUplinkName}}",
      "network": {
        "href": "{{networkHref}}"
      },
      "interfaceType": "uplink",
      "subnetParticipation": [
        {
          "gateway": "{{uplinkGateway}}",
          "subnetPrefixLength": "{{prefixLength}}",
          "ipAddress": "{{uplinkIpAddress}}",
          "ipRanges": null,
          "useForDefaultRoute": false,
          "totalIpCount": 0,
          "autoAllocateIpRanges": null
        }
      ],
      "applyRateLimit": false,
      "inRateLimit": null,
      "outRateLimit": null,
      "useForDefaultRoute": false,
      "connected": true,
      "quickAddAllocatedIpCount": null
    },
    "subnetValue": {
      "gateway": "{{externalNetworkGateway}}",
      "prefixLength": "{{externalNetworkprefixLength}}",
      "dnsSuffix": null,
      "dnsServer1": "",
      "dnsServer2": "",
      "enabled": true,
      "autoAllocateIpRanges": false
    }
  }
}